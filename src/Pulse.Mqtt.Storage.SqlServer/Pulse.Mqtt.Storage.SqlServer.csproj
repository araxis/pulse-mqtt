<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFrameworks>net8.0;net10.0</TargetFrameworks>
    <IsPackable>true</IsPackable>
    <PackageId>Pulse.Mqtt.Storage.SqlServer</PackageId>
    <Description>SQL Server-backed durable session and message stores for the Pulse MQTT client: subscriptions, the offline queue, and in-flight QoS state survive restarts.</Description>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Pulse.Mqtt.Core\Pulse.Mqtt.Core.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Data.SqlClient" />
  </ItemGroup>

</Project>
